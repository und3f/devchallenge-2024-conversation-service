{
  "type": "module",
  "scripts": {
    "test": "cucumber-js",
    "test-fast": "cucumber-js --tags 'not @long' && echo \"\nReady to use: http://localhost:8080\"",
    "test-long": "cucumber-js --tags '@long'"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^11.0.0"
  }
}
